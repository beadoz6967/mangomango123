#include "../pch.h"
#include "movement.hpp"
#include "../gui/gui.h"
#include <Windows.h>

namespace Movement {
    void Update() {
        if (GUI::bBhop) {
            // Bunny hop logic would go here
        }
        if (GUI::bStrafe) {
            // Strafe helper logic would go here
        }
    }
}
